{
  "name": "email-saas-platform-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "start:all": "concurrently \"npm start --prefix client\" \"npm start --prefix server\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
